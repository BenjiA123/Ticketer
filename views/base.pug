<!DOCTYPE html>
html 
    head 
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        title Bells Tickets | #{title}
        link(rel='stylesheet', href='/css/style.css')
        link(href='https://fonts.googleapis.com/css?family=Lato:300,300i,700'
        rel='stylesheet')

        script(src='https://polyfill.io/v3/polyfill.min.js?version=3.52.1&features=fetch')
        script(src='https://js.stripe.com/v3/')
        //- script(src='/script.js')


    body 
        //HEADER
        include _header


        //CONTENT
        block content 
            h1 What a placeholder
    
        //FOOTER
        include _footer
            //- This is here bacause the page has to load before I can manipulate it
            script(src='/js/script.js')